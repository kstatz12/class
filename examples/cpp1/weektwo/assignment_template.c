#include <stdlib.h>
int *get_pos(int *arry, size_t len, int target) {
  // search array for any 2 integers that when summed will equal the target
  // integer, return the array positions
  //
  int *pos = (int *)malloc(sizeof(int) * 2);

  return pos;
}
